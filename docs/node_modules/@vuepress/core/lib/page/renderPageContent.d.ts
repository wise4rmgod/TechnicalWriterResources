import type { MarkdownHeader, MarkdownLink } from '@vuepress/markdown';
import type { App, PageFrontmatter, PageOptions } from '../types';
/**
 * Render page content and extract related info
 */
export declare const renderPageContent: ({ app, content, filePath, filePathRelative, options, }: {
    app: App;
    content: string;
    filePath: string | null;
    filePathRelative: string | null;
    options: PageOptions;
}) => Promise<{
    contentRendered: string;
    deps: string[];
    excerpt: string;
    frontmatter: PageFrontmatter;
    headers: MarkdownHeader[];
    links: MarkdownLink[];
    sfcBlocks: string[];
    title: string;
}>;
