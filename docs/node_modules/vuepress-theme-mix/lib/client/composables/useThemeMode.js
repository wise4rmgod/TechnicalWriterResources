import { inject } from 'vue';
export const themeModeSymbol = Symbol('themeMode');
export const useThemeMode = () => {
    const themeMode = inject(themeModeSymbol);
    if (!themeMode) {
        throw new Error('useThemeMode() is called without provider.');
    }
    return themeMode;
};
