"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.preparePaletteFile = void 0;
const utils_1 = require("@vuepress/utils");
const preparePaletteFile = async (app, { userPaletteFile, tempPaletteFile, importCode, }) => {
    const userPalette = app.dir.source(userPaletteFile);
    let content = '';
    if (await utils_1.fs.pathExists(userPalette)) {
        content += importCode(userPalette);
    }
    return app.writeTemp(tempPaletteFile, content);
};
exports.preparePaletteFile = preparePaletteFile;
