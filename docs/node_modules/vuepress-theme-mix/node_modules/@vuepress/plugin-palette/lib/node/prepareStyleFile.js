"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareStyleFile = void 0;
const utils_1 = require("@vuepress/utils");
const prepareStyleFile = async (app, { userStyleFile, tempStyleFile, importCode, }) => {
    const userStyle = app.dir.source(userStyleFile);
    let content = '';
    if (await utils_1.fs.pathExists(userStyle)) {
        content += importCode(userStyle);
    }
    return app.writeTemp(tempStyleFile, content);
};
exports.prepareStyleFile = prepareStyleFile;
